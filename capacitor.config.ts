import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.desnecesauron.arlearnquest.app',
  appName: 'AR Learn Quest',
  webDir: 'dist'
};

export default config;
